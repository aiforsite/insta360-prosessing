requests>=2.31.0
Pillow>=10.0.0
opencv-python>=4.8.0
psutil>=5.9.0
nudged==0.3.1

# Optional (GPU-accelerated blur person detection):
# Install separately (large download), see `requirements-gpu.txt`.