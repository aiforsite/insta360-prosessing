requests>=2.31.0
Pillow>=10.0.0
opencv-python>=4.8.0
psutil>=5.9.0
nudged==0.3.1

# PyTorch / TorchVision:
# - Blur "GPU-assisted" person detection requires torch+torchvision.
# - Use PyTorch's CUDA wheel index so `pip install -r requirements.txt` installs GPU-capable wheels
#   (when supported by the machine + drivers).
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121